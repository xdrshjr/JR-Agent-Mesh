# 02 — 前端界面设计

## 1. 设计语言

### 风格定义

参照 Figma 的设计语言，核心原则：

- **简约**：大量留白，信息层级清晰，无多余装饰
- **优雅**：圆角（8px）、柔和阴影、平滑过渡动画（200ms ease）
- **专业**：中性色为主，功能色点缀，排版严谨

### 色彩系统

```
主色调（Brand）:
  primary:       #6366F1  (Indigo-500，用于主要操作按钮、选中状态)
  primary-hover: #4F46E5  (Indigo-600)
  primary-light: #EEF2FF  (Indigo-50，用于选中背景)

中性色（Neutral）:
  background:    #FFFFFF  (页面底色)
  surface:       #F9FAFB  (卡片/面板底色，Gray-50)
  border:        #E5E7EB  (分割线/边框，Gray-200)
  text-primary:  #111827  (主文本，Gray-900)
  text-secondary:#6B7280  (次级文本，Gray-500)
  text-muted:    #9CA3AF  (占位/禁用文本，Gray-400)

功能色：
  success:       #10B981  (Emerald-500)
  warning:       #F59E0B  (Amber-500)
  error:         #EF4444  (Red-500)
  info:          #3B82F6  (Blue-500)
```

### 字体

```
正文：Inter（西文）/ system-ui（中文回退）
代码：JetBrains Mono / Fira Code
大小：14px 基准，12px 辅助，16px 标题，20px 页面标题
行高：1.5（正文），1.4（代码）
```

## 2. 页面布局

### 整体结构

```
┌─────────────────────────────────────────────────────────┐
│  Top Bar (48px)                                          │
│  ┌──────┐  JRAgentMesh          ┌──────┐ ┌────┐ ┌────┐ │
│  │ Logo │                       │ 🔔   │ │ ⚙️ │ │ 🔊 │ │
│  └──────┘                       └──────┘ └────┘ └────┘ │
├────────────┬────────────────────────────────────────────┤
│            │                                            │
│  Sidebar   │  Main Content Area                         │
│  (240px)   │                                            │
│            │  ┌──────────────────────────────────────┐  │
│  ┌──────┐  │  │                                      │  │
│  │ Chat │  │  │   (根据当前路由/标签渲染内容)         │  │
│  │ 自身  │  │  │                                      │  │
│  │Agent  │  │  │                                      │  │
│  ├──────┤  │  │                                      │  │
│  │Agents│  │  │                                      │  │
│  │ 管理  │  │  │                                      │  │
│  ├──────┤  │  │                                      │  │
│  │设置   │  │  │                                      │  │
│  │      │  │  │                                      │  │
│  └──────┘  │  │                                      │  │
│            │  └──────────────────────────────────────┘  │
│  ┌──────┐  │                                            │
│  │状态栏 │  │                                            │
│  │连接/  │  │                                            │
│  │Agent数│  │                                            │
│  └──────┘  │                                            │
└────────────┴────────────────────────────────────────────┘
```

### 侧边栏（Sidebar）

- 宽度固定 240px，左侧垂直布局
- 顶部区域：导航菜单项（Chat / Agents / Settings）
- 中部区域：当前活跃的后台 Agent 列表（快捷入口）
- 底部区域：连接状态指示灯 + 活跃 Agent 数量

### 顶部栏（Top Bar）

- 高度 48px，水平布局
- 左侧：Logo + 项目名称 "JRAgentMesh"
- 右侧：通知铃铛（可展开通知面板）、声音开关、设置快捷入口

## 3. 页面详细设计

### 3.1 Chat 页面（自身 Agent 对话）

```
┌─────────────────────────────────────────────────────┐
│  Chat 页面                                           │
│                                                      │
│  ┌─ Model Selector ──────────────────────┐          │
│  │  Claude 3.5 Sonnet  ▼  │  调度模式 [OFF] │       │
│  └───────────────────────────────────────┘          │
│                                                      │
│  ┌─ 对话区域 ─────────────────────────────────────┐ │
│  │                                                 │ │
│  │  ┌─ User Message ───────────────────────────┐  │ │
│  │  │  请帮我创建一个 Python HTTP 服务器         │  │ │
│  │  │  📎 server.py (点击下载)                   │  │ │
│  │  └──────────────────────────────────────────┘  │ │
│  │                                                 │ │
│  │  ┌─ Assistant Message ──────────────────────┐  │ │
│  │  │  好的，我来帮你创建...                     │  │ │
│  │  │                                           │  │ │
│  │  │  ┌─ Timeline ──────────────────────────┐ │  │ │
│  │  │  │  ✅ read  config.json      0.2s     │ │  │ │
│  │  │  │  ✅ write server.py        0.5s     │ │  │ │
│  │  │  │  🔄 bash  python test.py   running  │ │  │ │
│  │  │  └─────────────────────────────────────┘ │  │ │
│  │  │                                           │  │ │
│  │  │  文件已创建，这是一个简单的 HTTP 服务...    │  │ │
│  │  │  📎 server.py (点击下载)                   │  │ │
│  │  └──────────────────────────────────────────┘  │ │
│  │                                                 │ │
│  └─────────────────────────────────────────────────┘ │
│                                                      │
│  ┌─ 输入区域 ─────────────────────────────────────┐ │
│  │  ┌──────────────────────────────┐ ┌──┐ ┌────┐  │ │
│  │  │  输入消息...                  │ │📎│ │发送│  │ │
│  │  │                              │ │  │ │  ↵ │  │ │
│  │  └──────────────────────────────┘ └──┘ └────┘  │ │
│  └─────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

**对话区域功能**：
- 消息列表支持自动滚动到底部，可手动上滑查看历史
- 用户消息右对齐浅灰背景，Assistant 消息左对齐白色背景
- Markdown 渲染（代码高亮、表格、列表等）
- 文件附件显示为可点击的卡片（图标 + 文件名 + 大小）

**Timeline 组件**：
- 嵌入在 Assistant 消息中，折叠/展开可切换
- 每一步显示：状态图标（✅🔄❌）+ 工具名 + 摘要参数 + 耗时
- 点击某一步可展开查看完整输入输出

**输入区域**：
- 多行文本输入框，支持 Shift+Enter 换行，Enter 发送
- 左侧附件按钮（📎），点击打开文件选择器
- 右侧发送按钮
- 输入框上方显示已选附件列表（可删除）

### 3.2 Agents 管理页面

```
┌─────────────────────────────────────────────────────┐
│  Agents 管理                                         │
│                                                      │
│  ┌─ 工具栏 ───────────────────────────────────────┐ │
│  │  + 新建 Agent ▼   │  筛选: 全部 | 运行中 | 已停止 │ │
│  └────────────────────────────────────────────────┘ │
│                                                      │
│  ┌─ 标签栏 ──────────────────────────────────────┐  │
│  │ [Claude Code #1 🟢] [OpenCode #1 🟢] [Codex #1 ⚫] + │
│  └───────────────────────────────────────────────┘  │
│                                                      │
│  ┌─ 当前 Agent 详情面板 ─────────────────────────┐  │
│  │                                                │  │
│  │  ┌─ 信息栏 ─────────────────────────────────┐ │  │
│  │  │ Claude Code #1    状态: 运行中 🟢          │ │  │
│  │  │ 工作目录: /home/user/project               │ │  │
│  │  │ PID: 12345  运行时长: 1h 23m               │ │  │
│  │  │ [停止] [重启]                              │ │  │
│  │  └──────────────────────────────────────────┘ │  │
│  │                                                │  │
│  │  ┌─ 输出/交互区域 ──────────────────────────┐ │  │
│  │  │                                           │ │  │
│  │  │  (终端风格的输出显示，ANSI 颜色渲染)       │ │  │
│  │  │                                           │ │  │
│  │  │  > 正在读取 src/index.ts ...              │ │  │
│  │  │  > 编辑文件 src/utils.ts                  │ │  │
│  │  │  > 执行命令: npm test                     │ │  │
│  │  │  > ✅ 所有测试通过                         │ │  │
│  │  │                                           │ │  │
│  │  └──────────────────────────────────────────┘ │  │
│  │                                                │  │
│  │  ┌─ 输入框 ─────────────────────────────────┐ │  │
│  │  │  向此 Agent 发送指令...           [发送]   │ │  │
│  │  └──────────────────────────────────────────┘ │  │
│  │                                                │  │
│  └────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
```

**新建 Agent 弹窗**：

```
┌─ 创建新 Agent ──────────────────────────┐
│                                          │
│  Agent 类型:  [Claude Code ▼]            │
│                                          │
│  名称 (可选):  [                    ]    │
│                                          │
│  工作目录:     [/home/user/project  ] 📁 │
│               (留空使用默认目录)           │
│                                          │
│  初始指令 (可选):                         │
│  ┌──────────────────────────────────┐   │
│  │  帮我重构 src/utils.ts 文件      │   │
│  └──────────────────────────────────┘   │
│                                          │
│           [取消]  [创建并启动]            │
│                                          │
└──────────────────────────────────────────┘
```

**标签栏行为**：
- 每个标签显示 Agent 名称 + 状态指示灯（🟢运行中 / 🟡启动中 / ⚫已停止 / 🔴崩溃）
- 标签可通过拖拽重新排序
- 右键标签弹出菜单（停止 / 重启 / 删除 / 复制名称）
- 最右侧 `+` 按钮快速创建新 Agent

### 3.3 Settings 页面

```
┌─────────────────────────────────────────────────────┐
│  设置                                                │
│                                                      │
│  ┌─ 分类导航 ─────────────────────────────────────┐ │
│  │  [自身Agent] [后台Agent] [通知] [通用]          │ │
│  └────────────────────────────────────────────────┘ │
│                                                      │
│  ── 自身 Agent 设置 ──                               │
│                                                      │
│  默认模型                                            │
│  ┌──────────────────────────────────────────────┐   │
│  │  Provider: [Anthropic ▼]                      │   │
│  │  Model:    [claude-sonnet-4-5-20250929 ▼]     │   │
│  └──────────────────────────────────────────────┘   │
│                                                      │
│  自定义 LLM                                          │
│  ┌──────────────────────────────────────────────┐   │
│  │  API URL:  [https://api.example.com/v1    ]   │   │
│  │  API Key:  [sk-••••••••••••••••           ]   │   │
│  │  Model ID: [custom-model-name             ]   │   │
│  └──────────────────────────────────────────────┘   │
│                                                      │
│  System Prompt                                       │
│  ┌──────────────────────────────────────────────┐   │
│  │  你是一个有用的 AI 助手...                     │   │
│  │                                               │   │
│  └──────────────────────────────────────────────┘   │
│                                                      │
│  ── API 凭证 ──                                      │
│                                                      │
│  ┌─────────────────────────────────────────────┐    │
│  │  Anthropic API Key   [sk-••••••] [编辑] [删除]│   │
│  │  OpenAI API Key      [sk-••••••] [编辑] [删除]│   │
│  │  + 添加凭证                                   │   │
│  └──────────────────────────────────────────────┘   │
│                                                      │
│                                    [保存设置]        │
└─────────────────────────────────────────────────────┘
```

## 4. 组件树

```
App (layout.tsx)
├── MobileGuard                        // 移动端检测，显示不支持提示
├── WebSocketProvider                  // WebSocket 连接上下文
├── TopBar
│   ├── Logo
│   ├── ProjectTitle
│   ├── NotificationBell
│   ├── SoundToggle
│   └── SettingsShortcut
├── Sidebar
│   ├── NavMenu
│   │   ├── NavItem (Chat)
│   │   ├── NavItem (Agents)
│   │   └── NavItem (Settings)
│   ├── ActiveAgentList               // 快捷显示运行中的 Agent
│   │   └── AgentQuickCard (×N)
│   └── ConnectionStatus
└── MainContent
    ├── [/chat] ChatPage
    │   ├── ModelSelector
    │   │   ├── ProviderDropdown
    │   │   ├── ModelDropdown
    │   │   └── DispatchToggle         // 调度模式开关
    │   ├── ConversationList           // 历史会话列表（侧面板）
    │   ├── MessageArea
    │   │   ├── MessageBubble (×N)
    │   │   │   ├── MarkdownRenderer
    │   │   │   ├── ToolTimeline       // 工具调用 Timeline
    │   │   │   │   └── ToolStep (×N)
    │   │   │   └── FileAttachment (×N)
    │   │   └── TypingIndicator
    │   └── InputArea
    │       ├── TextInput
    │       ├── AttachmentButton
    │       ├── AttachmentPreview (×N)
    │       └── SendButton
    ├── [/agents] AgentsPage
    │   ├── AgentToolbar
    │   │   ├── CreateAgentButton
    │   │   └── StatusFilter
    │   ├── AgentTabBar
    │   │   └── AgentTab (×N)
    │   ├── AgentDetailPanel
    │   │   ├── AgentInfoBar
    │   │   ├── AgentOutputArea        // 终端风格输出
    │   │   │   └── AnsiRenderer
    │   │   └── AgentInputBox
    │   └── CreateAgentDialog          // 新建弹窗
    │       ├── AgentTypeSelector
    │       ├── WorkDirInput
    │       └── InitialPromptInput
    └── [/settings] SettingsPage
        ├── SettingsTabs
        ├── SelfAgentSettings
        │   ├── ModelConfig
        │   ├── CustomLLMConfig
        │   └── SystemPromptEditor
        ├── BackendAgentSettings
        │   └── AgentTypeConfigs (×N)
        ├── NotificationSettings
        │   ├── SoundToggle
        │   └── BrowserNotificationToggle
        └── GeneralSettings
            ├── DataExport
            └── DataImport
```

## 5. 移动端守卫

当检测到屏幕宽度 < 768px 时，渲染全屏提示页面：

```
┌──────────────────────────┐
│                          │
│      JRAgentMesh         │
│                          │
│    🖥️                    │
│                          │
│  请使用桌面设备访问       │
│                          │
│  JRAgentMesh 暂不支持    │
│  移动设备。请在电脑上     │
│  打开此页面。             │
│                          │
└──────────────────────────┘
```

## 6. 交互与动画

| 交互 | 动画 |
|------|------|
| 侧边栏导航切换 | 选中项背景色渐变 150ms |
| 标签页切换 | 内容区域淡入 200ms |
| 新消息出现 | 从底部滑入 + 淡入 250ms |
| Timeline 展开/折叠 | 高度过渡 200ms ease |
| 弹窗打开/关闭 | 缩放 + 淡入 200ms |
| Agent 状态变更 | 状态灯闪烁 2 次后稳定 |
| 通知弹出 | 从右侧滑入 300ms，3s 后自动消失 |

## 7. 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+Enter` | 发送消息（输入框 focused 时） |
| `Ctrl+N` | 新建对话 |
| `Ctrl+K` | 聚焦搜索/命令面板 |
| `Ctrl+1/2/3` | 切换侧边栏 Chat/Agents/Settings |
| `Escape` | 关闭弹窗/面板 |
