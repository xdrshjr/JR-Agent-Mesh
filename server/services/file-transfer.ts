import { logger } from '../utils/logger.js';

// Stub — will be implemented in 08-file-transfer
export class FileTransferService {
  constructor() {
    logger.info('FileTransferService', 'Initialized (stub)');
  }
}
